[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra

# Legacy V5 files named ``*_test.py`` are executable research scripts rather
# than tests for the architecture kernel.  Keeping discovery explicit makes
# ``python -m pytest`` reproducible without importing their optional SciPy
# runtime.
