# 从第一原理理解“动态临床状态”

> 本文先推导问题，不先选择数据结构或算法。以下原则与反例在候选比较前作为输入冻结；研究结束后的架构选择见第 19 节和 `DECISION.md`。后续若修改原则、反例或硬要求，必须升版本并重跑受影响测试，不能无痕回写成“原先就如此”。

## 1. 我们究竟在表示什么

患者在任何时刻都有某种真实但不可完全见到的生理状态。系统接触到的不是该状态本身，而是带条件、延迟、误差和来源的记录：一次测量、一段陈述、一项操作、一份报告。医学知识再把这些记录约束成若干仍可能并存的解释。

因此，下面四件事不能是同一个对象：

1. **世界发生了什么**：潜在生理状态与真实事件；
2. **我们看到了什么**：观察、陈述、文档和它们的测量条件；
3. **系统据此认为可能是什么**：计算、推断、假说与不确定性；
4. **我们对世界做了什么**：已实施干预，以及尚未实施的计划。

如果把它们压进一个“当前患者向量”，系统会失去区分“正常血压”和“升压药维持下的正常血压”的能力，也会让最终诊断倒流成早期证据。

## 2. 第一原则一：知识不是世界，记录也不是知识

### 推导

- 世界状态不可直接访问；任何输入都经过传感、采样、陈述或记录过程。
- 同一个世界状态可以产生不同观察；同一个观察也可由不同状态产生。
- 所以核心必须显式保存**观察过程**，而不是只保存观察值。

### 反例

血氧均为 98%，一人室内空气，一人高流量氧疗。若表示相同，治疗安全和严重度判断发生临床上重要的碰撞。

### 结果

核心至少要区分潜在状态命题、原始证据、派生断言和干预；观察需要上下文与可靠度。

## 3. 第一原则二：临床知识具有多重时间

### 推导

“什么时候发生”和“什么时候有资格被系统知道”是不同问题。检验结果可在采样数小时后返回，迟录病历可描述更早事件，知识规则也会在未来修订。

若只存一个时间戳，就无法同时正确回答：

- 患者当时可能处于什么状态；
- 当时医生能知道什么；
- 用今天的知识怎样重新解释旧证据。

### 结果

至少需要事件/有效时间与知识/可见时间两条相互独立的顺序；采集、记录、过期可作为明确角色或区间。任何历史读出必须以可见时间作截断。

## 4. 第一原则三：缺失、否定和不适用不是一个值

### 推导

没有记录可能意味着未询问、未检查、无法评价、记录丢失或确实不存在。这些状态对诊断证据的方向完全不同。

### 反例

病历未提皮疹，不能等于医生检查并明确“无皮疹”。若统一为 0，系统会制造阴性证据。

### 结果

不确定性不仅是数值上的概率；**信息状态**本身必须可枚举、可冲突、可保留多个解释，并允许“模型外/不可表示”。

## 5. 第一原则四：证据只能贡献一次独立信息

### 推导

“体温 40.0°C”“体温升高”“高热”可能是同一测量的三种表示。若它们独立投票，改写次数就会改变结论，而不是新事实改变结论。

### 结果

- 每个派生结论必须保留对原始来源的依赖集合；
- 独立性不能由文字是否不同决定；
- 删除或撤回源证据，所有仅依赖它的派生物必须失效；
- 推断产物不得回流成为自己的独立证据。

这要求的是证据血缘与依赖维护，而不只是解释文本。

## 6. 第一原则五：干预同时改变世界和观察通道

### 推导

治疗可能改变内部机制，也可能只改变外在读数，或同时改变两者。例如抗菌药降低病原负荷并降低培养阳性率；升压药提高血压但不必立即消除导致休克的机制。

### 结果

核心需要分别表达：

- 对潜在状态转移的作用；
- 对观察生成/可见性的作用；
- 实际实施的动作与计划动作；
- 动作的开始、剂量、持续与停止。

把治疗简单写成某轴加减，会混淆“病好了”和“表现被遮蔽”。

## 7. 第一原则六：组合不是默认相加

### 推导

若两个模块各自定义局部效应，它们共存时可能竞争同一资源、互相门控、饱和、协同或改变观察过程。简单线性叠加只是一种可声明的组合律，不是普遍事实。

### 结果

核心需要：

- 明确组合边界和交互点；
- 对无已知交互、已知非线性交互和冲突模型给不同语义；
- 交互知识可局部添加，而不是重写全局引擎；
- 无组合律时显式拒绝静默近似。

## 8. 第一原则七：任务坐标是读出，不是本体

诊断、危重程度、治疗安全和预后关注不同信息，也承担不同错误代价。不存在理由要求它们共享唯一距离、唯一排序或唯一低维坐标。

固定内核应保存任务无关的事件、证据、机制假说与不确定性；每个任务通过带版本和血缘的读出生成临时坐标。读出不能反过来修改事实层。

## 9. 第一原则八：开放世界要求“拒绝”是正常结果

医学知识永远不完备。一个封闭分类器即使对所有输入都返回已知疾病，也没有回答输入是否在模型支持范围内。

核心必须能表示：

- 多个解释并存；
- 没有已知解释足够支持；
- 证据不足以区分；
- 输入违反契约或概念尚不存在。

“检索没找到”只能说明本次检索未召回，不能成为不存在的证据。

## 10. 第一原则九：可重放要求事实与知识都可版本化

给定同一事件历史，需要两种不同但都合理的重放：

1. **当时视角**：只用当时可见证据和当时生效知识；
2. **今天重释**：仍只用截至目标时点发生的证据，但允许使用今天的知识版本。

若不版本化证据可见性、规则与术语映射，这两种问题会被混在一起，审计无法复现。

## 11. 第一原则十：固定内核应约束错误，而不是收纳全部医学词汇

疾病、检查和药物会持续增加；若新增概念需要新增核心对象类型或执行分支，所谓固定内核并不固定。相反，若核心只有一个无类型“节点/边”，非法状态会被静默接纳。

因此最小性不是“字段最少”，而是：用尽量少的**有语义类型的原语与组合律**，让常见危险状态不可构造或可被验证器拒绝。

## 12. 第一原则十一：可计算性必须与可审计性共存

系统可以使用连续模型、采样、神经网络或近似推断，但这些不能成为唯一事实存储。任何任务读出至少要能列出：

- 使用了哪些原始证据；
- 证据在该时点为何可见且未过期；
- 哪个版本的知识和参数参与；
- 哪些依赖被视为同源；
- 哪些冲突、未知和近似仍存在。

这不要求每个数值都能用一句自然语言完全解释，但要求计算路径和语义承诺可复查。

## 13. 第一原则十二：双时间是底线，不是全部临床时间

valid time 与 transaction time 解决“现实何时成立”和“系统何时保存”这两个正交问题，但临床流程还会出现采集、实验室接收、审核发布、某个角色实际可见、记录和过期。它们不能靠字段名字或先后直觉互相推断。

并发事件也不总有唯一全序：两条独立记录可同一时刻产生，只有明确的依赖、通信或 workflow 边才能制造 happens-before。核心应把时间角色类型化，并允许偏序；排序键只能服务执行，不能偷偷变成因果。

## 14. 第一原则十三：血缘不等于统计独立性

provenance 可以回答某个结论使用了哪些源以及经过什么变换，却不会自动回答两个源是否条件独立。两个不同记录可能来自同一标本；同一个设备也可在不同时间产生相对独立的重复测量。

因此系统至少需要两个不同问题：

1. **身份与派生**：哪些节点最终来自同一个 root evidence；
2. **相关/独立假设**：这些 root 之间在当前模型下如何共同贡献。

按根身份幂等去重是安全底线；把根数直接当独立票数仍可能错误，必须在 trace 中暴露 source family 或相关模型。

## 15. 第一原则十四：因果可表达不等于因果可识别，程序能运行不等于结果有效

把观察和干预写成两个操作只是必要条件。治疗效果还依赖混杂、选择、可交换性、positivity、模型结构等识别假设；个体反事实还要求事实世界与反事实世界共享同一患者的外生状态，而不是重新抽一个人。

同理，概率程序、方程或反馈网络可能多解、无解、不收敛或漏掉小概率但关键的 mode。核心不能把求解失败压成普通数值；识别状态、coverage、收敛诊断和近似边界必须与临床结论一起返回。

## 16. 第一原则十五：最小性和稳定性有条件，不是绝对禁改

只有一个 `Node(payload)` 的系统表面原语最少，但若所有角色、时间和合法关系都藏进自由文本与解释器分支，它比显式类型更复杂、更危险。因此要统计的不只是 class 名，还包括强制关系、metamodel、特殊分支、自由回调和非法状态表面积。

同样，“核心永远不改”不是目的。普通新疾病、药物或检查不应增加核心 opcode；但若出现真正新的时间或证据语义，升级一个有版本和迁移路径的核心可能比把新语义塞进 payload 更正确。

## 17. 第一原则十六：架构正确性只能由反例和不变量限定

单个成功病例不能证明架构。一个候选若依赖未来信息、把未知当阴性或允许推断自证，即使平均准确率高也不合格。

所以研究先冻结硬性不变量，再统一测试候选；性能和便利性只能在通过硬约束的候选之间比较。冻结不表示反例永远不变，而表示任何测试后修订都要留下版本、原因和前后结果，旧面板不得继续冒充新合同的证据。

## 18. 第一原则十七：封闭边界与有限资源也是语义

“禁止 callback”如果只写在文档里，而运行边界仍接受对象子类、任意 `Mapping`、反射、可执行字符串或带钩子的序列化对象，就没有真正封闭。攻击者或意外实现可在验证、复制、哈希或序列化时执行宿主代码，也可用超深、循环或超大结构让验证器本身失去可用性。

因此公共边界必须：

- 只接受精确、惰性的封闭传输类型和有 discriminator 的有限构造子；
- 在复制、哈希和解释之前拒绝对象子类、callable、循环、非有限数值和未知 opcode；
- 对深度、节点数、字节数、求解步数、时间和内存给出有限预算；
- 把预算耗尽、超时、无解、多解和不收敛返回为不同的 typed result，而不是空值、最近近似或普通 `ok`；
- 让 foreign operator 走独立、版本化、可审计的 capability boundary，不能借 `payload:any` 绕过。

资源预算不是医学真理，但会改变“该次计算是否有资格被信任”，所以属于结果合同的一部分。

## 19. Pre-bridge 研究结论与重开状态

由以上推导和本仓库对 **13 个候选家族、3 个原子原型及共同测试面板**的比较，2026-07-13 的 pre-bridge 最小共同承诺基线是一个类型化可信控制面 `K0`：

1. 认识论角色、稳定身份、作用域和信息状态的合同；
2. 多角色时间、版本和查询 cut；
3. 可撤回的证据根/派生 witness，以及与统计独立性分开的 provenance；
4. 封闭查询与正交的 capability、coverage、identification、computation 和 safety 结果；
5. 封闭输入边界、有限资源预算和 fail-closed 不变量。

`K0` 只固定这些合同、身份、cut 和审计边界；它**不再保存第二份权威事实**，也不定义唯一 `PatientState`、动力学或组合律。完整参考联邦另需一个唯一的 evidence-authority 实现（本仓库以 TEL/TMS 为参考）、状态/动力学、因果和 typed rewrite/open-machine 能力子内核；缺哪一项，相应查询就 typed `unsupported`，不得由别的 calculus 静默代答。

这里的“最小”和“没有单一候选足够”都只是在上述候选集、原型和反例范围内的经验性架构结论。我们给出了删除见证和消融条件，但**没有完成数学不可约性或全局最优证明**。2026-07-14 的冻结 bridge 轮已出现实现层 hard counterexample，并使 Checkpoint 3/7 重开；因此 K0 现在只保留为仍需反驳/替代的需求基线，不是冻结赢家。
