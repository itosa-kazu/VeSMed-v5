# 研究状态

> 读法：Checkpoint 1–8 中未显式写“当时”的“当前”，均指该 checkpoint 形成时的时点；仓库当前裁决以最后的 Checkpoint 9 为准。

## Checkpoint 1 — 2026-07-13（已完成）

- **当前假说**：问题的必要内核可能不是单一数值状态，而是“类型化、双时间、证据守恒的可组合转移语义”；这只是待证假说，不是选型。
- **已完成**：建立研究边界和评价顺序；完成独立第一原理红队；把主体/标本作用域、多角色时钟、观察机会、来源与独立性分离、因果可识别性、动作生命周期、求解失败、反馈学习等遗漏纳入 `REQUIREMENTS.md` v0.2；将“最小性”从硬门槛降为 Pareto 指标，避免用一个无类型万能原语作弊。
- **当前最强候选是什么？** 本检查点不选型。当前待证假说是：任何合格方案都要显式保存认识论状态，并把世界动力学与证据状态分开。
- **最强反对意见是什么？** 所谓“小内核”可能只是把复杂性推到知识模块、规则 DSL 或投影层，并未降低总语义负担。
- **是否有候选因根本缺陷应被淘汰？** 只淘汰具体“唯一核心”版本：无类型单状态、无多时间资格、把未知当阴性、把计划当实施或无法隔离未来信息的版本。仍可把其数值/存储能力保留为组件。
- **是否需要扩大搜索空间？** 当前覆盖计划包含状态、图、概率、因果、事件/逻辑、过程/反应、类型/重写、事件溯源、组合数学、世界模型等；完成第一轮后复查。
- **证据是否只支持 Pareto 前沿？** 当前默认很可能如此，但必须由统一原型实验决定。

## Checkpoint 2 — 2026-07-13（已完成）

- **当前假说**：候选不应按品牌名计数，而应按“状态对象 + 演化/更新算子 + 查询语义”去重；工具、存储格式、推断后端和安全元层不能冒充新的患者动态家族。
- **已验证**：已完成状态/概率、因果/PPL、事件/双时间、过程/反应、类型/重写、组合数学、图/世界模型等独立调查；测试公平性审查发现原 T01–T50 偏审计语义，已补 E01–E08 动态/因果/组合实验与 Native/Companion 消融协议。
- **当前最强候选是什么？** 尚无单一赢家。Temporal Evidence Ledger 在时间/回放/血缘上强；SSM/SCM 在动态/干预上不可替代；typed rewrite/open machines 在局部组合与失败闭合上有潜力。
- **最强反对意见是什么？** 以上优势可能证明的是多个互不相容的子内核，而不是一个更小的统一核心；混合方案也可能只是堆层。
- **是否有候选因根本缺陷应被淘汰？** 静态向量、普通 Transformer latent、裸知识图、普通 PPL、ECS/黑板、裸范畴/operad 作为**唯一固定核心**已出现结构性缺口；它们仍可保留为投影、实现、推断或组合组件。
- **是否需要扩大搜索空间？** 已加入 open-universe、hybrid automata、temporal Datalog/TMS、typed open machines 和 sheaf consistency；暂不继续按名称扩张，先完成去重和判别实验。
- **证据是否只支持 Pareto 前沿？** 文献阶段是；原型尚未运行，不能提前把 Pareto 前沿收缩为一个赢家。

## Checkpoint 3 — 2026-07-13（原已完成；2026-07-14 重开）

- **当时假说**：最小共同内核不是统一患者状态，而是一个可信语义控制面 `K0`：typed artifact/scope、多角色时间与版本 cut、grounded proof/撤回、封闭 query、正交 Outcome、capability 与 fail-closed invariant。
- **当时已验证**：TEL、causal/state、typed rewrite/open 的“状态、更新、时间、等价、血缘、替代历史”不可无损统一；撤回不等于反向生理转移，conditioning 不等于 `do`，历史重放不等于个体反事实，evidence provenance 不等于 causal/execution trace。
- **当时最强候选是什么？** `K0 + 明确分家的能力子内核` 是当时最强的可运行假说；bridge 轮后来使选择重开。
- **最强反对意见是什么？** `K0` 可能只是接口治理而不是用户所问的“状态怎样产生”的完整核心；能力子内核之间的 bridge 可能比直接混合更复杂。
- **是否有候选因根本缺陷应被淘汰？** 任何允许 `Node(payload)`、`Rule(fn)`、任意 callback/SQL/prompt/bytecode 穿过模块边界，并把能力计成一个原语的实例硬淘汰。
- **是否需要扩大搜索空间？** 暂否。下一项信息增益最高的是机器实验，而不是增加更多架构名称。
- **证据是否只支持 Pareto 前沿？** 是。K0 只是共同约束；TEL、causal-state、rewrite-open 仍保留各自 Pareto 语义。

## Checkpoint 4 — 2026-07-13（已完成）

- **当前假说**：可信比较需要先证明 fixture 与 judge 能区分“正确行为、诚实拒绝、错误答案和伪造轨迹”，而不是只比较候选自报 capability。
- **已验证**：冻结 T01–T50 与 E01–E08 共 58 个机器 workload；每项分离 `candidate_view` 与 runner-only `oracle_view`，同时加入正向 liveness、semantic eligibility、完整轨迹坐标、外部 fresh replay 和行为/边界/trace/numerical 分轴裁决。T35 以封闭 recurrence oracle 拒绝稀疏轨迹和 25 点固定垃圾。
- **当前最强候选是什么？** 本检查点的产物是判别工具，不是新架构候选；它当前支持继续比较 TEL、causal/state、rewrite/open，而不提前奖励任何一家。
- **最强反对意见是什么？** 58 个 fixture 仍由本研究团队设计，公开后可能被迎合；reference model 只是有限 toy，不能代表真实临床模型和数据分布。
- **是否有候选因根本缺陷应被淘汰？** 这一阶段淘汰的是无判别力的测试做法：只看 manifest、把任意 `unsupported` 当 PASS、只核对轨迹端点、信任候选自己的 `clean_rebuild`、或让候选看到 oracle。
- **是否需要扩大搜索空间？** 暂不扩大候选家族；最有信息增益的是独立 hidden holdout、mutation 与 bridge round-trip，而不是继续增加名称。
- **证据是否只支持 Pareto 前沿？** 是。workload 能暴露能力覆盖和失败边界，但不同面板的适用项不同，原始 PASS 数不能直接相加成总排名。

## Checkpoint 5 — 2026-07-13（已完成）

- **当前假说**：三个原生语义可各自正确实现，但不能在不丢语义的情况下压成一个统一 `PatientState/update`；K0 只能路由和守边界，不能凭接口制造缺失能力。
- **已验证**：实现 3 个原子原型：TEL、finite dynamic causal/state、typed rewrite/open；实现 `ClinicalKernel` 作为联邦/K0 判别仪器，并实现 `ExperimentalModelSubkernel` 执行 E01–E08 公共模型。v3 隔离面板中，TEL 为 `36 PASS / 20 HONEST_UNSUPPORTED / 2 FAIL`，rewrite 为 `31/23/4`，causal 为 `3/53/2`；kernel 为 `36/20/2`，model instrument 为 `13/44/1`。这些计数只描述该实现对本面板的行为，不能跨不同原生适用域当总分。
- **当时最强候选是什么？** 没有原子赢家；当时最强可运行假说是 `K0 + evidence/time、state/dynamics、causal、rewrite/open safety` 的显式分家联邦。
- **最强反对意见是什么？** 原型深度和公开 workload 覆盖不均：causal 原型刻意不提供 ledger 语义，TEL 不提供数值 dynamics；kernel/model 又是判别仪器而非独立候选，不能把其 PASS 反记给任一原子家族。
- **是否有候选因根本缺陷应被淘汰？** 三个原型作为**唯一完整固定核心**均硬淘汰；其原生能力保留为非支配子内核。Companion 赛道没有凭适配器获得免费语义。
- **是否需要扩大搜索空间？** 不需要先做第四个品牌原型；应先独立验证 evidence→model bridge 是否保持 root/time/version/uncertainty。
- **证据是否只支持 Pareto 前沿？** 是。TEL 的时间/血缘、causal/state 的过滤/干预/AAP、rewrite/open 的可执行安全与组合是不同 Pareto 轴。

## Checkpoint 6 — 2026-07-13（已完成）

- **当前假说**：若 runner 边界不可信，漂亮的候选结果没有证据价值；红队修复必须同时收紧 harness 和候选输入/输出合同。
- **已验证**：红队击穿同进程 oracle 隔离、unsupported 假 PASS、稀疏 trajectory、万能拒绝、候选自报 rebuild，以及对象子类/callback、masked raw 泄漏、causal future-effective、rewrite 历史/动作顺序/数值冲突等路径。v1 保留为红队前诊断基线；v2 引入 fresh `python -I -S` 进程隔离但仍有文件系统探测面；v3 加入 CPython audit hook、最小读 allowlist，并阻断 subprocess/network/native-loader。
- **当时最强候选是什么？** 经修订后，K0 的 typed input、closed query/result、cut/witness 与 fail-closed invariant 当时比任何“相信候选自报正确”的方案更有支持。
- **最强反对意见是什么？** v3 是针对已审计 pure-Python 候选的 Python-level confinement，**不是 OS sandbox**；恶意 native code、解释器漏洞或操作系统侧信道不在证明范围。
- **是否有候选因根本缺陷应被淘汰？** v1 结果不能用于决策，v2 只作过渡诊断；接受 callback/任意对象图、泄漏 mask、忽略时间 cut、或把计划动作当执行的实现版本被淘汰。
- **是否需要扩大搜索空间？** 架构家族无需扩大；若威胁模型包含恶意第三方代码，执行隔离必须扩大到独立 OS/container/VM，并由另一个团队重跑。
- **证据是否只支持 Pareto 前沿？** 是。红队提高了证据可信度，但没有消除语义能力、实现复杂度、性能和可审计性的权衡。

## Checkpoint 7 — 2026-07-13（原已完成；2026-07-14 重开）

- **当时假说**：当时最有支持的固定核心不是患者动力学本身，而是条件性最小的可信控制面 `K0`；患者动力学与因果/安全计算由明确声明能力的异构子内核承担。
- **当时已验证**：13 家族调查、删除反例、3 原子原型、58-workload v3 隔离面板和红队一致否决单一原子赢家。当时冻结 `K0=(TypedArtifactRole, IdentityAndScope, TypedTimeAndCut, VersionedDerivationAndProvenance, ClosedQueryAlgebra, ProductResultAndFailure, InvariantEnforcement)` 作为研究基线。
- **当时最强候选是什么？** `K0 + 唯一 evidence authority + state/dynamics + causal + typed rewrite/open safety` 的受控联邦；bridge 反证后它不再是冻结赢家。
- **最强反对意见是什么？** K0 可能只是接口治理，不回答患者如何演化；bridge 可能隐藏同等甚至更大的医学语义和维护成本，使“最小核心”只是复杂性搬家。
- **是否有候选因根本缺陷应被淘汰？** “一个向量/图/概率程序/规则机天然包办事实、动力学、因果、证据和安全”的唯一核心宣传版本被淘汰；这些技术仍可在其原生边界内保留。
- **当时下一步**：优先做双实现 bridge round-trip holdout；其失败已使 Checkpoint 3/7 重开。
- **当时证据是否只支持 Pareto 前沿？** 是。受控联邦和条件性 K0 只是 Pareto 研究基线，不是全局最优、唯一性或形式不可约性证明。

## Checkpoint 8 — 2026-07-13（历史交付完成）

- **当时假说**：只有当结论、形式规范、原型、原始结果、失败、扩展包和一键验证同时落盘，研究才算可复查，而不是一篇架构意见。
- **当时已验证**：完成 `ARCHITECTURE.md` 两张图、`FORMAL_SPEC.md`、决策/威胁/红队文档、演示 JSON、4 类 typed extension、58-workload v3 原始面板和 metrics 快照；当时全量 QA 为 `119 passed, 7 subtests passed`。运行时注册扩展无需改 fixed-core 文件、旧结果语义 delta 为 0、extension-id 专属分支为 0；候选文件的静态 test/oracle dispatch 扫描也为 0。前三个扩展没有 authoring 前快照；第四个 test-method 扩展的静态 before/after 显示新增 extension 文件 1、fixed-core 改动 0（harness 修改另记 5 个文件）。
- **当时最强候选是什么？** 当时可交付的研究参考物是 K0 受控联邦及其 3 个原子原型；`ClinicalKernel` 和 model subkernel 是可运行证据，不是临床产品。
- **最强反对意见是什么？** 当时仍未有独立团队、完整可执行 hidden holdout、真实世界校准/效益、目标规模性能、权限隐私、人因或监管验证；119 个测试不能替代这些证据。
- **是否有候选因根本缺陷应被淘汰？** 所有把本交付描述成“临床已验证”“生产安全”“全局最优”或把 `HONEST_UNSUPPORTED` 当能力完成的说法都被明确排除。
- **是否需要扩大搜索空间？** 交付阶段无需；下一研究迭代应扩大验证者、数据、规模和 bridge holdout，而不是偷偷给当前原型加病例特例。
- **证据是否只支持 Pareto 前沿？** 是；完整交付让 Pareto 与限制可复查，但不改变结论强度。

## Checkpoint 9 — 2026-07-14（执行轮已封存；完成门禁未达成）

- **当前假说**：冻结 A/B 的实现层 hard counterexamples 已击穿 bridge-stability 主张；sealed 41-case corpus 本身没有产生 `CANDIDATE_FAIL`，而是因 fixture/runner 不完整必须单独标记 `HARNESS_INCOMPLETE`。两层证据不可互相补偿。
- **已验证**：seal→reveal→hash 顺序和静态 source-distinct 下界成立。corpus 层：A mechanical 为 `0 PASS / 4 ADAPTER_UNREPRESENTABLE / 37 HARNESS_INCOMPLETE`，B corrected run-03 为 `0 PASS / 41 HARNESS_INCOMPLETE`，所以只支持 `HARNESS_INCOMPLETE`。实现层：单独记账的 42 个确定性 post-seal probe 为 `15 pass / 26 fail / 1 partial`，尤其 B 的 M01 execution/recovery split-brain，以及 A 的三通道 typed-result 缺失、B 的 evidence/record uncertainty sidecar 不活跃等 hard counterexamples，支持 `HYPOTHESIS_FAIL`。B 的 model-level 三通道参数扰动是 live 的；数值 filter/smooth 与 B 的 condition/do/AAP 子路径也可运行，但不能补偿上述 hard failure。bridge 专项为 `16 passed`，当前全量 QA 为 `135 passed, 7 subtests passed`；最终报告与机器摘要位于 `results/bridge-holdout/REPORT.md`、`final-report.json`。
- **当前最强候选是什么？** 没有 bridge 候选通过完整门禁；K0 的 invariant requirements 仍是研究基线，但“当前 bridge 已证明稳定”的固定核心选择已撤回。
- **最强反对意见是什么？** corpus 只有 4 个 concrete base、35 个 descriptor-only case，并有 3 组 dangling query；因此不能把两个实现的失败上升为 K0 family 或所有 versioned bridge 的不可能性定理。
- **是否有候选因根本缺陷应被淘汰？** 淘汰冻结 A/B 作为合规 bridge 的主张；不淘汰整个 K0 family。M01 在两者均 `NOT_KILLED`，且 B 存在 native execution 与 recovery tape 分裂。
- **是否需要扩大搜索空间？** 是，重开 Checkpoint 3/7。先冻结 append-only、完整可执行的 operational probe pack，再并行比较 richer typed bridge、统一 typed event/probabilistic/causal calculus 和更窄 control plane。
- **证据是否只支持 Pareto 前沿？** 是，而且 bridge 维度的证据目前不足以冻结任何赢家。完整报告见 `results/bridge-holdout/REPORT.md`。
