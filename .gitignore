__pycache__/
*.py[cod]
.pytest_cache/
tmp_pmc/
tmp_*.json

# Expanded UCM raw rows remain available locally, but the desktop Git panel
# must not index hundreds of thousands of JSONL lines.  Deterministic gzip
# sidecars are checked in and verify against the original manifest digests.
results/unified_map/**/raw-*.jsonl
results/unified_map/**/.*.tmp/
# Superseded pre-canonical demo attempt; the replacement bundle is tracked.
results/unified_map/demo/20260719T065722Z-DEMO-65f4649b88/
