# 固定核心架构要求与不变量

> 版本：0.3（最终研究输入；保留 Checkpoint 1 红队修订）  
> 冻结规则：本表在最终候选比较前冻结。测试后发现的缺陷可以形成带版本的新要求，但必须记录变更并重跑受影响面板；不得改写旧要求后继续引用旧结果。  
> 判定规则：硬约束失败不能由平均准确率、易实现或额外特例抵消。

## 1. 等级定义

- **HARD**：违反即淘汰当前架构实例；若候选家族必须改变基本原语才能修复，则淘汰该家族版本。
- **SOFT**：影响 Pareto 位置，可用明确代价换取。
- **ANTI**：明确禁止用作“解决方案”的做法。

## 2. 硬性要求

| ID | 要求 | 最小反例 | 可运行不变量 |
|---|---|---|---|
| R-EPI-01 | 真实/潜在状态、原始观察、主体陈述、确定性计算、医学推断、诊断假说、已实施动作、计划动作必须类型可区分 | 计划升压药被当作已给药 | 非法角色替换被拒绝；计划动作不改变状态 |
| R-EPI-02 | 派生结论不得变成自己的独立证据 | “发热→感染→感染支持发热”循环增信 | 依赖图不得存在会增加支持质量的自证闭环 |
| R-EPI-03 | 一般医学知识、患者特异证据、任务政策/效用和模型输出必须有不同作用域 | 指南语句被当作该患者已发生事实 | 跨作用域消费需显式、合法的转换；模型输出不回写成 raw evidence |
| R-SCOPE-01 | 患者、就诊、标本、设备、部位与来源身份不可因概念同名而合并 | 两位患者或两个标本的 creatinine 被粘合 | subject/specimen/scope 不匹配在组合前失败；跨就诊关联有 provenance |
| R-TIME-01 | 至少区分事件/有效时间与系统可见/知识时间 | 8:00 采血，12:00 出结果，9:00 回放用了结果 | `as_known_at=09:00` 不得出现 12:00 才可见的结果 |
| R-TIME-02 | 支持采集、记录、有效和过期语义，不将时间缺失静默猜测成同一时点 | 旧血氧永久代表当前状态 | 过期观察不进入当前读出，仍保留审计记录 |
| R-TIME-03 | 规则、术语与模型版本可重放 | 新理论修改后无法复现旧结论 | 当时知识重放结果确定；今天重释明确标注新版本 |
| R-TIME-04 | 发生/有效、采集、结果可用、记录/事务、过期等时钟角色不可被一个无角色时间戳替代 | 08:00 采样、12:00 发布、14:00 复制记录 | 每个时钟字段有类型；缺失时钟保持 unknown，不互相猜测 |
| R-TIME-05 | 并发事件允许偏序；同一时间戳不应凭存储顺序制造因果 | 两条独立记录同毫秒到达 | 只有显式 happens-before/依赖边产生顺序语义 |
| R-OBS-01 | 观察值必须绑定方法、条件、支持治疗、单位、可靠度和适用主体/部位的可扩展上下文 | 高流量氧下 SpO2 98% 与室内空气混同 | 上下文差异不得被核心规范化成同一语义状态 |
| R-OBS-02 | 观察生成过程与潜在状态转移分离 | 升压药下正常 MAP 被推成休克机制消失 | 干预可独立作用于 state transition 与 observation channel |
| R-OBS-03 | 观察机会、采样选择、检测限和未记录过程可显式表示；观察窗外“无记录”不自动提供否定证据 | 未采培养却因无阳性结果推成无感染 | missingness/ascertainment scope 可审计；超检测限/低于检测限不是普通数值 |
| R-UNC-01 | 明确否定、未询问、未检查、无法评价、证据不足、冲突、不适用、模型外必须可区分 | 未提皮疹被当作无皮疹 | 缺失输入不产生阴性证据；冲突输入两侧均保留 |
| R-UNC-02 | 支持多个互斥或重叠解释并存，不强制单分类 | 同表型两种机制 | 输出可保留多个假说及各自未决条件 |
| R-UNC-03 | 信息状态、数值概率、适用性、模型覆盖和计算诊断是正交维度，不压进单个“置信度” | 证据冲突但模型 posterior 恰为 0.5 | trace 分别报告 polarity/missing reason/conflict、probability、coverage 与 solver status |
| R-UNC-04 | 局部矛盾不得逻辑爆炸，也不得 last-write-wins 静默覆盖 | 同时记录 rash 与 no-rash | 冲突只影响依赖该命题的结论；无关命题不能由矛盾任意推出 |
| R-PROV-01 | 每个派生坐标/结论 100% 回指原始证据、规则/模型版本和中间依赖 | “高热”无原始体温来源 | trace 中不存在无来源的派生支持 |
| R-PROV-02 | 同一原始证据的多个改写不得伪装为独立证据 | 40°C、发热、高热三票 | 复制或改写派生物不改变独立来源计数/支持质量 |
| R-PROV-03 | 撤回原始证据会递归失效仅依赖它的推断，且不会删除仍有独立支持的结论 | 删除体温后“高热”仍有效 | truth maintenance 结果与依赖集合一致 |
| R-PROV-04 | 同一 source event 重复摄取必须幂等；身份由稳定来源标识决定而非载荷文本 | 接口重试导致同一 lab 到达两次 | 重放/重试不增加证据根或改变结论；真正独立复测仍保留两个根 |
| R-PROV-05 | provenance 描述来源与推导，但“统计独立”必须是另一个显式、可审计的假设 | 同一标本两台分析仪 vs 两个独立标本 | 不凭不同记录 ID 自动宣称独立；聚合 trace 显示 source-family/相关策略 |
| R-INT-01 | 计划、下单、开始、实施、暂停、停止的干预状态不可混同 | 已开药但未服用 | 只有满足实施契约的事件可改变状态/观察模型 |
| R-INT-02 | 干预可分别修改潜在动力学、观察通道和参数；不能默认只改读数或只改病程 | 抗菌药改变病原负荷也改变培养灵敏度 | 两类效应可分别声明与追踪 |
| R-INT-03 | 观察条件化、外部干预与同一患者反事实查询必须是不同操作；因果结论绑定识别假设 | 病重者更常接受治疗且结局更差 | `observe(A=a)` 不可替代 `do(A=a)`；无法识别或数值失败时返回显式状态 |
| R-INT-04 | 若系统声称治疗选择，患者目标、偏好、效用、约束、授权和安全 invariant 与事实/风险推断分离 | 最大生存概率被自动当成唯一最佳治疗 | action query 明示目标与约束；缺失时不输出“最优治疗” |
| R-INT-05 | 动作生命周期支持请求、执行、部分实施、拒绝、暂停、取消、停止和幂等重试 | 取消医嘱仍持续施加 effect | effect 仅覆盖实际实施区间；重复 action event 不重复施效 |
| R-COMP-01 | 模块局部扩展；增加疾病、药物、检查不修改固定核心类型或主执行分支 | 新药需要 `if drug_name` | extension blast radius 中核心修改数应为 0；不支持则显式失败 |
| R-COMP-02 | 支持声明非线性、门控、饱和、竞争和协同；没有组合律时不得静默默认相加 | CKD 与药物清除非线性交互 | 未声明的冲突组合返回 unresolved/unsupported |
| R-COMP-03 | 模块接口必须带语义类型、方向、主体身份、单位、时钟与上下文契约；同名不等于可连接 | 把两个患者的 creatinine port 因同名粘合 | 接口不兼容在加载/组合时显式失败 |
| R-COMP-04 | 交互的“未建模、未检索、证据冲突、采用近似、已有无重要交互证据”必须可区分 | 知识库没写 interaction 被当成 interaction=0 | 使用加性/独立近似时 trace 列出假设与适用域；超出声明闭包返回 unsupported |
| R-TASK-01 | 底层记录可生成不同任务的版本化临时投影，任务读出不得回写事实层 | 诊断和用药安全需要不同坐标 | 两任务输出可不同且共享可追踪底层证据 |
| R-OPEN-01 | 开放世界：无已知模型足够解释时允许拒绝；检索未召回不得变成否定事实 | 新疾病被强塞进最近模板 | unknown/out-of-model 是显式合法结果，不要求硬分类 |
| R-REP-01 | 任意历史点可按当时可见信息确定性重放；未来信息严格隔离 | 最终诊断泄漏到首诊 | replay trace 中所有节点 `known_from <= cutoff` |
| R-REP-02 | 分开 `replay_as_then` 与 `reinterpret_now`：前者固定当时知识，后者用新版知识重释旧事实 | 新指南发布后回顾旧病例 | API/trace 明确模式，二者不会静默混用 |
| R-REP-03 | 重放绑定规则、模型、术语、参数、随机源和外部响应快照；缺失必要版本时显式失败 | 回放时调用当前外部服务得新结果 | 同输入与同快照产生语义等价结果；不要求跨平台 bitwise 相同 |
| R-SAFE-01 | 无法表达的输入显式报错或隔离，不静默丢字段/近似 | 新测量方法被当旧方法 | 契约验证失败带原始载荷和原因 |
| R-SAFE-02 | 关键安全规则的执行不依赖语义检索是否召回 | 禁忌规则未被检索 | 标记为 invariant 的规则在适用域内确定性执行 |
| R-SAFE-03 | 求解器多解/无解、采样不收敛、近似超界或模型适用域失败不得伪装成普通结论 | 循环方程有两个固定点 | 返回 typed diagnostic/unsupported，trace 保留失败与近似信息 |
| R-BOUND-01 | 所有公共输入、query、module 和 bridge 必须通过封闭、带 discriminator 的类型边界；不得接受 callback、反射、可执行字符串、任意对象子类或未声明 foreign operator | `Rule(fn)` 在验证/复制时读取 oracle 或执行网络请求 | 在任何复制、哈希、序列化或解释前拒绝非 exact-builtin 传输图、循环、未知 opcode 和 callable；foreign boundary 单独版本化计账 |
| R-BOUND-02 | 输入验证和求解必须有显式有限资源预算，预算耗尽是独立 typed failure | 超深 JSON 让验证器递归耗尽，或无界 reachability 假装仍在计算 | 深度/节点/字节/时间/步数/内存超限 fail-closed；不得返回部分值为普通 `ok`，不得静默提高预算 |
| R-ID-01 | 临床语义相同的自然语言改写在规范化后不产生无医学意义差异，同时保留原文 | 同一病例换措辞改变核心状态 | 语义规范化结果同构；原始来源不同可审计 |
| R-ID-02 | 临床上必须不同处理的状态不得碰撞 | 有/无氧疗的相同 SpO2 | collision 测试为 0 个硬碰撞 |
| R-ID-03 | 单位、方法和概念转换必须由版本化显式规则完成；无合法转换时拒绝合并 | mg/dL 与 μmol/L 或不同 assay 被直接平均 | trace 含转换规则和输入；不兼容方法不静默归一 |
| R-ID-04 | 规范化必须可逆地保留原值、原单位、原文/source span 和映射版本 | 标准化后无法复核原报告 | round-trip trace 可回到原始载荷；更换术语映射不篡改旧版本 |
| R-KNOW-01 | 普通医学内容扩展不得新增核心 opcode；核心语义升级必须版本化并保留迁移/回放策略 | 新 assay 触发执行器 `if assay_name` | 新疾病/药物/检查核心分支修改数为 0；真正新语义走显式内核版本 |
| R-LLM-01 | LLM/NLP 输出默认是不可信 proposed artifact；结构验证不等于医学验证，必须保留 source span/适配 provenance | LLM 将“未提发热”补成 `fever=false` | 未验证提取不能伪装 raw observation；可撤回且回指原文/输入 |
| R-LEARN-01 | 在线更新、政策反馈和治疗污染若存在，必须版本化、可回滚，并区分自然史与策略诱导数据 | 模型推荐治疗后把治疗后表现学成疾病自然轨迹 | 每次模型/政策版本与暴露范围可追踪；旧历史不被静默重写 |

## 3. 软性要求与 Pareto 指标

| ID | 维度 | 测量方式 |
|---|---|---|
| S-EXP-01 | 表达完整性 | 统一测试中无需语义损失即可表示的情形比例 |
| S-MIN-01 | Core primitive count | 固定对象类型 + 基本操作 + 内建组合律数量，逐项列出而非只报总数 |
| S-CASE-01 | Special-case count | 为通过特定测试新增、不能普遍复用的分支/规则数 |
| S-EXT-01 | Extension blast radius | 新疾病/药物/检查各需修改的核心文件、核心分支、既有模块数 |
| S-PROV-01 | Trace completeness | 输出中有完整原始证据与规则版本血缘的结论比例，目标 100% |
| S-REP-01 | Replay correctness | 历史回放与期望快照一致比例；硬用例必须 100% |
| S-COLL-01 | Collision count | 临床需不同处理但核心表示相同的用例数 |
| S-SPUR-01 | Spurious distinction count | 临床语义相同但因措辞/格式被拆开的用例数 |
| S-CYCLE-01 | Illegal cycle count | 自证、同一时刻无语义固定点的增信循环数 |
| S-UNK-01 | Unknown handling | 模型外、证据不足、不可表示三类是否分别可输出并追踪原因 |
| S-AUDIT-01 | Human audit burden | 审核一个结论需查看的 trace 节点数、跨文件跳转数和不可解释计算数 |
| S-AUTH-01 | 知识编写负担 | 新模块 schema 字段数、必须手填参数数、跨模块协调数 |
| S-LLM-01 | LLM 填充负担 | 契约可验证字段比例、自由文本语义瓶颈数、无效输出可定位性 |
| S-COST-01 | 运行成本 | 全量重算与单事件增量更新的时间/内存复杂度及实测 |
| S-COMP-01 | 自然组合性 | 非线性交互、共病、观察修饰无需内核特例的覆盖率 |
| S-MIN-02 | 语义最小性 | 每个原语的不可约性；删除后硬测试变化；同时统计隐藏 metamodel、解释器分支和自由回调，不只数 class 名 |
| S-STAB-01 | 核心稳定性 | 常规内容扩展耗时、需迁移范围、需重跑范围；不奖励把语义藏进无类型 payload |

## 4. 反目标

| ID | 禁止事项 | 原因 |
|---|---|---|
| A-01 | 把 LLM 输出直接当语义真相 | 生成模型不能保证类型、时序、来源和安全不变量 |
| A-02 | 把检索/QKV/缓存当存在性或否定性语义 | 未召回不等于不存在，优化不能决定事实 |
| A-03 | 用一个无类型万能图宣称问题已解决 | 可容纳不等于约束了合法状态与操作 |
| A-04 | 用隐向量作为唯一事实来源 | 无法完整回放、撤回和证明证据守恒 |
| A-05 | 用疾病专属 `if`、加分、硬门或重复证据修排名 | 破坏几何/机制公平与局部可组合性 |
| A-06 | 把未观测当 0、false 或正常 | 制造虚假阴性证据 |
| A-07 | 为实现方便合并多重时间 | 导致未来泄漏和不可复现审计 |
| A-08 | 用漂亮 UI、单病例成功或旧结果文件证明架构 | 不能验证内核不变量和泛化 |
| A-09 | 用加权总分掩盖硬失败 | 安全不变量不可与速度或代码量互相补偿 |
| A-10 | 先选赢家再定测试或给赢家更多规则预算 | 造成确认偏差和不可比实验 |
| A-11 | 用 core primitive count 单项决定赢家 | 一个无类型 `Node(payload)` 可把全部复杂度藏进 payload 与解释器 |
| A-12 | 把执行 trace 当临床 evidence provenance | 程序调用栈不自动证明原始证据身份、独立性或可见时间 |

## 5. 冻结后的控制面与完整参考能力

### 5.1 `K0` 最小共同控制面

`K0` 固定合同而不保存第二份权威事实，至少包括：

1. 类型化 artifact/action/knowledge 角色、身份和作用域；
2. valid/effective、available/known、recorded/transaction 等多时间角色与版本 cut；
3. evidence root、依赖 witness、幂等和撤回/重放合同；
4. 封闭 query 类型与正交的 unknown/conflict/capability/coverage/identification/computation/safety 结果；
5. module/bridge/foreign boundary 的封闭注册、版本、适用域与 capability origin；
6. exact-builtin 传输验证、有限资源预算和 fail-closed 不变量。

满足这六项只表示**共同控制面合规**，不表示拥有患者动力学、因果反事实、可达性或完整临床计算能力。

### 5.2 完整参考联邦的必需能力

本研究的完整参考 profile 需要四组语义能力；它们可以在物理部署中合并进少数进程，但不得抹去能力边界或复制权威事实：

1. **唯一 evidence authority（TEL/TMS 参考实现）**：持久化原始事实/action/knowledge 版本、knowledge cut、root provenance、撤回、`replay_as_then` / `reinterpret_now` 和任务投影；K0 只规定其 port/合同；
2. **state/dynamics**：显式分开状态转移与观察生成，提供 filter/smooth/forecast 和 coverage/numerics；
3. **causal**：分开 condition、population `do` 与共享外生背景的同患者 counterfactual，并报告 identification；
4. **typed rewrite/open machine**：动作 typestate、局部变迁、端口/wiring、reachability、feedback 与安全 invariant。

降级必须是局部且可见的：没有 evidence authority 时运行时不构成临床事实系统；没有 state/dynamics 时 filter/smooth/forecast 为 `unsupported`；没有 causal 时 `do`/counterfactual 为 `unsupported`；没有 rewrite/open 时动作执行语义、reachability、组合与相应 safety 检查为 `unsupported`。任何其余子内核都不得用近似查询静默代答。

这里的“最小”是对本研究 13 个家族、3 个原型和冻结反例的经验性共同边界；尚无数学不可约性证明。删除消融、隐藏 holdout 或新 calculus 可推翻它。

## 6. 部署硬边界（本 PoC 不声称通过）

以下不是可由架构单元测试证明的“核心原语”，但任何临床部署都不能省略：

- 目标用途下的临床关联、分析验证和临床验证；
- 概率校准、可迁移性、亚组公平性、漂移监测和撤销资格；
- 隐私、同意、用途限制、访问控制、完整性、灾备和责任链；
- 对被遮蔽/无权限信息的正确语义：不可见不等于不存在或阴性；
- 人员审批、操作规程、监管 intended use 和人机责任边界。

原型通过 `R-*` 与 `T*` 只证明在定义范围内的表示/执行不变量，不证明真实诊断准确性、治疗获益或生产安全。
